# Core — required for basic depth inference
comfy-env==0.2.9
safetensors>=0.4.0,<1.0.0

# 3D preview viewer (used by prestartup_script.py)
comfy-3d-viewers==0.2.28

# Optional — auto-download models from HuggingFace
huggingface_hub

# Optional — Gaussian Splatting SH rotation (giant model)
e3nn>=0.5.0

# Optional — 3D export (point cloud, mesh)
trimesh
plyfile>=1.0.0
DracoPy
pygltflib
pyfqmr

# Optional — streaming mode alignment
scipy>=1.10.0
scikit-learn>=1.3.0

# Optional — streaming loop closure
pypose>=0.6.0
faiss-cpu
numba>=0.58.0
